# Site Ownership Map (Public surfaces)

This page mirrors the canonical ownership map from `wrensignal/wrenOS/docs/site-ownership.md`.

## Authoritative source per surface

- `https://wrenos.ai/` → source: `wrensignal/0xClaw-site` (`index.html`)
- `https://wrenos.ai/docs` → source: `wrensignal/0xClaw-site` (`docs/index.html`)
- Core implementation truth (CLI/runtime contracts) → `wrensignal/wrenOS`

## Sync policy

1. Capability changes land in `wrensignal/wrenOS` first.
2. Public copy on `wrenos.ai` updates in `wrensignal/0xClaw-site` after core merge.
3. No speculative feature claims on site pages without merged core evidence.

## Canonical reference

- https://github.com/wrensignal/wrenOS/blob/main/docs/site-ownership.md
